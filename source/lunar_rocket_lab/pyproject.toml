[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lunar-rocket-lab"
version = "0.1.0"
description = "Isaac Lab direct workflow tasks for LunarRocket."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]

